# Robinhood Chain (chain id 4663). Take the values from the Robinhood Chain docs; nothing is hard-coded.
ROBINHOOD_RPC_URL=
USDG_ADDRESS=
EXPLORER_API_URL=
EXPLORER_API_KEY=

# Deployment parameters (script/Deploy.s.sol)
LANCEFIELD_OWNER=0x
LANCEFIELD_REGISTRAR=0x
LANCEFIELD_FEE_BPS=0
LANCEFIELD_FEE_RECIPIENT=0x0000000000000000000000000000000000000000
# Optional: reuse an existing AgentRegistry instead of deploying a new one.
LANCEFIELD_REGISTRY=

# Deployer key (or use --ledger / --account instead)
PRIVATE_KEY=
